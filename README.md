A backend project built using FastAPI to manage categories and products.
This API supports basic CRUD operations, pagination, and a one-to-many relationship between categories and products.

Make sure to configure your .env file with your database URL before running.